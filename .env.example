# Copy this file to `.env` and replace the placeholder with your AssemblyAI API key.
# The server will read this variable at startup. Never commit a real API key to
# version control.

ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here
